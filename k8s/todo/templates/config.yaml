apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "todo.fullname" . }}-config
  labels:
    app: {{ template "todo.fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
data:
{{- range $key, $val := .Values.config }}
  {{ $key }}: {{ $val | quote }}
{{- end }}