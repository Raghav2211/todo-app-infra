# My SQL Database
spring.datasource.driver = com.mysql.jdbc.Driver
spring.datasource.url = jdbc:mysql://127.0.0.1:3306/klab
spring.datasource.username = root
spring.datasource.password = root
spring.jpa.show-sql = true
spring.jpa.hibernate.ddl-auto = update
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect

## Configure info endpoint
info.app.name=TODO App on Kubernetes application
info.app.description=TODO App on Kubernetes application
info.app.version=@project.version@
info.app.java.source=@java.version@
info.app.java.target=@java.version@

# configure health endpoint
management.endpoints.health.sensitive=false
management.health.db.enabled=true
management.health.defaults.enabled=true
management.endpoint.health.show-details=always
# enable all actuator endpoints
management.endpoints.web.exposure.include=*
 
# build version
klab.todo.build.version=@project.version@
